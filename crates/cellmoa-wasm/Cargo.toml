[package]
name = "cellmoa-wasm"
description = "WebAssembly bindings for the cellmoa engine"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
cellmoa-api.workspace = true

[features]
default = []
